#include <stdio.h>

int main() {
    int myAge = 25;
    int votingAge = 18;
    
    printf("%d", myAge >= votingAge); // Mengembalikan 1 (benar), artinya anak berusia 25 tahun diperbolehkan memilih!
    
    return 0;
}