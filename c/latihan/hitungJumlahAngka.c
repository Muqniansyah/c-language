#include <stdio.h>

// buat fungsi
void calculateSum() {
    int x = 5;
    int y = 10;
    int sum = x + y;
    printf("Jumlah x + y adalah: %d", sum);
}

int main() {
    calculateSum();  // memanggil fungsi

    return 0;
}